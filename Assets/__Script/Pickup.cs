using UnityEngine;
using System.Collections;

public class Pickup : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void PickupItem () {
		
	}

	void OnTriggerStay2D(Collider2D other) {
//		if (Input.GetKeyDown (KeyCode.Space) && other.CompareTag ("magicItems")) {
//			_interactObj = other.GetComponent <Interact_Obj>();
//			// add magic item to inventory
//			_catInventory = _interactObj._magicItemsID;
//			// call PickedUp function in Interact_Obg
//			_interactObj.PickedUp (gameObject.transform);
//		}
	}
}
